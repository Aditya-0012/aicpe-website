{
  "name": "aicpe",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "serverless-http": "^4.0.0"
  }
}